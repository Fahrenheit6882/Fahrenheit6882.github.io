# AGENTS.md — Fahrenheit Robotics Team 6882 Website

This file captures project context for LLM assistants working in this repository.

---

## Project Overview

**Fahrenheit Robotics** is the public website and knowledge base for FIRST Robotics Team 6882 (Fredericksburg area). It serves two audiences:

- **Public visitors** — team info, contact, social links (root `index.html`)
- **Team members** — guides on software, mechanical, CAD, project management, and website development (Docusaurus docs site at `/site/`)

Live site: https://fahrenheitrobotics.org

---

## Repository Structure

```
Fahrenheit6882.github.io/
├── index.html          # Public-facing landing page (W3.CSS, NOT Docusaurus)
├── app.css             # Landing page styles
├── img/                # Images used by the landing page
├── CNAME               # Domain: fahrenheitrobotics.org
├── CHANGELOG.md        # Semantic-versioned release notes
├── scripts/            # Manual deployment scripts (deploy0.sh, deploy1.sh)
└── site/               # Docusaurus source — ALL content work happens here
    ├── docs/           # Documentation articles
    ├── blog/           # Newsletter/blog posts
    ├── src/            # React components, pages, CSS
    ├── static/         # Static assets (images, fonts)
    ├── docusaurus.config.ts
    ├── sidebars.ts
    └── package.json
```

---

## Tech Stack

| Layer | Technology |
|---|---|
| Static site generator | Docusaurus 3.6.3 |
| UI library | React 18 |
| Language | TypeScript ~5.2.2 |
| Package manager | **Yarn** (not npm) |
| Markdown format | MDX 3.0 (Markdown + JSX) |
| CSS framework | Infima (Docusaurus default) |
| Code highlighting | Prism React Renderer 2.3.0 |
| Landing page | Plain HTML + W3.CSS |

Node.js >= 18.0 is required. Always use `yarn` — there is a `yarn.lock` tracked in git.

---

## Branch Strategy

| Branch | Purpose |
|---|---|
| `docusaurus` | **Active development branch** — all editing happens here |
| `main` | Production branch — contains the *built* site output |
| `gh-pages` | GitHub Pages deployment branch |

**Do all content and code work on the `docusaurus` branch.**

---

## Common Commands

All commands run from the `site/` directory:

```bash
cd site
yarn start        # Local dev server with hot reload
yarn build        # Production build → site/build/
yarn serve        # Preview the production build locally
yarn clear        # Clear Docusaurus cache (fixes stale build issues)
```

---

## Content Organization

### Docs (`site/docs/`)

```
docs/
├── welcome.md                     # Entry point (sidebar_position: 1)
├── software/                      # Git, JavaScript, robot code, VSCode, CAD
├── mechanical/                    # Mechanical engineering notes
├── project_management/            # Task and project management guides
└── marketing/website/             # Website dev guides (setup, org, deploy)
```

Sidebar is **auto-generated** from the folder structure (`sidebars.ts` uses `autogenerated`). Control order and labels via front matter:

```yaml
---
sidebar_label: My Label
sidebar_position: 2
---
```

### Blog (`site/blog/`)

Each post lives in its own directory:

```
blog/
├── authors.yml                    # Author profiles
└── 2025-03-28-newsletter/
    ├── index.md                   # Post content
    └── img/                       # Post images
```

Blog post front matter:

```yaml
---
slug: newsletter-issue-1
title: Newsletter Issue 1
authors: [upgradingdave]
tags: [newsletter]
---
```

Author `upgradingdave` = Dave Paroulek (Mentor). Add new authors to `site/blog/authors.yml`.

---

## Key Configuration Files

- **`site/docusaurus.config.ts`** — site title, URL, navbar, footer, theme, plugins
- **`site/sidebars.ts`** — sidebar structure (currently auto-generated)
- **`site/src/css/custom.css`** — theme color overrides (primary green: `#2e8555` light / `#25c2a0` dark)
- **`site/src/pages/index.tsx`** — redirects Docusaurus root → `/site/docs/welcome`
- **`CNAME`** — domain config (`fahrenheitrobotics.org`)

The Docusaurus site is served at the `/site/` base path (not `/`). The root `/` is the standalone `index.html` landing page.

---

## Deployment Process (Manual)

Deployment is currently **manual** (no CI/CD yet — it's a tracked TODO in CHANGELOG.md).

1. Build: `cd site && yarn build`
2. Run staging scripts: `bash scripts/deploy0.sh && bash scripts/deploy1.sh`
   - Copies `site/build/` → staging area along with `img/`, `index.html`, `app.css`
3. Switch to `main`: `git checkout main`
4. Replace `site/`, `img/`, and root HTML files with staged content
5. Push: `git push origin main`
6. Tag the release: `git push origin <NEW_TAG>`

See `site/docs/marketing/website/website-deploy.md` for the full step-by-step guide.

---

## Naming Conventions

| Context | Convention | Example |
|---|---|---|
| Doc directories | snake_case | `project_management/` |
| Doc filenames | kebab-case | `website-deploy.md` |
| Blog post dirs | `YYYY-MM-DD-slug/` | `2025-03-28-newsletter/` |
| React components | PascalCase | `HomepageFeatures` |
| CSS modules | kebab-case + `.module.css` | `styles.module.css` |

---

## Things to Know

- **Do not modify `index.html` or `app.css`** for doc content changes — those files belong to the separate landing page, not the Docusaurus site.
- **Images in docs** live in `img/` subdirectories alongside the `.md` files and are referenced relatively (e.g., `![alt](img/foo.png)`).
- **Images in static/** (`site/static/img/`) are for assets referenced from config or components.
- The `site/build/` directory is gitignored — it is the Docusaurus build output.
- A `.nojekyll` file at the repo root disables Jekyll processing on GitHub Pages.
- Docusaurus version is **3.x** — use v3 docs, not v2.
